[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Disable health check during initial deploy (model download takes time)
# Re-enable after first successful deploy if desired
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
numReplicas = 1
